@model CrmCorner.Models.PipelineTask
@using Microsoft.AspNetCore.Mvc.Rendering
@using CrmCorner.Models.Enums

@{
    ViewData["Title"] = "Yeni Görev Oluştur";
    Layout = "~/Views/Shared/_LayoutHomePage.cshtml";
}

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

@if (!ViewData.ModelState.IsValid)
{
    <div class="alert alert-danger">
        <ul>
            @foreach (var key in ViewData.ModelState.Keys)
            {
                foreach (var error in ViewData.ModelState[key].Errors)
                {
                    <li>@error.ErrorMessage</li>
                }
            }
        </ul>
    </div>
}

@if (TempData["DebugMessage"] != null)
{
    <div class="alert alert-warning">
        @TempData["DebugMessage"]
    </div>
}

<form asp-action="PipelineTaskCreate" method="post">
    @Html.AntiForgeryToken()

    <div class="row">
        <div class="col-lg-6">
            <div class="form-group">
                <label asp-for="Title">Başlık</label>
                <input asp-for="Title" class="form-control" required />
                <span asp-validation-for="Title" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Value">Değer Teklifi</label>
                <div class="input-group">
                    <input asp-for="Value" class="form-control" />
                    <select asp-for="Currency" class="form-control" style="max-width: 20%;">
                        <option value="₺">₺</option>
                        <option value="$">$</option>
                        <option value="€">€</option>
                    </select>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Description">Açıklama</label>
                <textarea asp-for="Description" class="form-control" rows="4"></textarea>
            </div>

            <div class="form-group">
                <label asp-for="Stage">Aşama</label>
                <select asp-for="Stage" asp-items="ViewBag.StageList" class="form-control" required>
                    <option value="">Lütfen seçiniz</option>
                </select>
                <span asp-validation-for="Stage" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="ExpectedCloseDate">Planlanan Satış Tarihi</label>
                <input asp-for="ExpectedCloseDate" type="date" class="form-control" />
            </div>
        </div>

        <div class="col-lg-6">
            <div class="form-group">
                <label asp-for="CustomerName">Ad</label>
                <input asp-for="CustomerName" class="form-control" required />
                <span asp-validation-for="CustomerName" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="CustomerSurname">Soyad</label>
                <input asp-for="CustomerSurname" class="form-control" required />
                <span asp-validation-for="CustomerSurname" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="CompanyName">Şirket Adı</label>
                <input asp-for="CompanyName" class="form-control" required />
                <span asp-validation-for="CompanyName" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Phone">Telefon</label>
                <input asp-for="Phone" class="form-control" placeholder="5xxxxxxxxx" maxlength="10" pattern="\d{10}" />
            </div>

            <div class="form-group">
                <label asp-for="Email">E-posta</label>
                <input asp-for="Email" type="email" class="form-control" required />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="LinkedinUrl">LinkedIn URL</label>
                <input asp-for="LinkedinUrl" class="form-control" />
            </div>

            <div class="form-group">
                <label asp-for="SourceChannel">Bu görüşme kaydını nereden edindiniz?</label>
                <select asp-for="SourceChannel" asp-items="ViewBag.SourceChannels" class="form-control" required>
                    <option value="">Lütfen seçiniz</option>
                </select>
                <span asp-validation-for="SourceChannel" class="text-danger"></span>
            </div>

            <hr />
            <h5>Kişiler</h5>

            @if (ViewBag.UseAppUser == true)
            {
                <div class="form-group">
                    <label asp-for="AppUserId">Görüşmeyi Alan</label>
                    <select asp-for="AppUserId" class="form-control"
                            asp-items="@(ViewBag.Users as List<SelectListItem>)">
                        <option value="">Lütfen seçiniz</option>
                    </select>
                    <span asp-validation-for="AppUserId" class="text-danger"></span>
                </div>
            }

            @if (ViewBag.UseResponsibleUser == true)
            {
                <div class="form-group">
                    <label asp-for="ResponsibleUserId">SDR</label>
                    <select asp-for="ResponsibleUserId" class="form-control"
                            asp-items="@(ViewBag.Users as List<SelectListItem>)">
                        <option value="">Lütfen seçiniz</option>
                    </select>
                    <span asp-validation-for="ResponsibleUserId" class="text-danger"></span>
                </div>
            }

            @if (ViewBag.UseMeetingUser == true)
            {
                <div class="form-group">
                    <label asp-for="MeetingUserId">Görüşmeyi Gerçekleştiren</label>
                    <select asp-for="MeetingUserId" class="form-control"
                            asp-items="@(ViewBag.Users as List<SelectListItem>)">
                        <option value="">Lütfen seçiniz</option>
                    </select>
                    <span asp-validation-for="MeetingUserId" class="text-danger"></span>
                </div>
            }

            @if (ViewBag.UseReporterUser == true)
            {
                <div class="form-group">
                    <label asp-for="ReporterUserId">Raporlamacı</label>
                    <select asp-for="ReporterUserId" class="form-control"
                            asp-items="@(ViewBag.Users as List<SelectListItem>)">
                        <option value="">Lütfen seçiniz</option>
                    </select>
                    <span asp-validation-for="ReporterUserId" class="text-danger"></span>
                </div>
            }
        </div>
    </div>

    <br />
    <div class="form-group">
        <input type="submit" value="EKLE" class="btn btn-primary" />
    </div>
</form>

<script src="~/CRMTema/bootstrap5/dist/assets/bundles/libscripts.bundle.js"></script>
<script src="~/CRMTema/bootstrap5/dist/assets/bundles/mainscripts.bundle.js"></script>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
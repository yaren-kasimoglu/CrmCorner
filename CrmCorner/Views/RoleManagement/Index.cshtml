@* @model List<CrmCorner.ViewModels.UserRoleViewModel>

@{
    ViewData["Title"] = "Kullanıcı Yetkilendirme";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<table class="table table-bordered table-hover bg-white">
    <thead class="table-dark">
        <tr>
            <th>#</th>
            <th>Email</th>
            <th>Şirket</th>
            <th>Mevcut Rol</th>
            <th>Yeni Rol</th>
        </tr>
    </thead>
    <tbody>
        <form asp-action="UpdateRoles" method="post">
            @for (int i = 0; i < Model.Count; i++)
            {
            <tr>
                <td>@(i + 1)</td>
                <td>@Model[i].Email</td>
                <td>@Model[i].CompanyName</td>
                <td>@Model[i].CurrentRole</td>
                <td>
                    <input type="hidden" name="[@i].UserId" value="@Model[i].UserId" />
                    <select name="[@i].NewRole" class="form-select form-select-sm">
                            @foreach (var role in Enum.GetNames(typeof(CrmCorner.Models.Enums.UserRole)))
                            {
                            <option value="@role" @(Model[i].CurrentRole == role ? "selected" : "")>@role</option>
                            }
                    </select>
                </td>
            </tr>
            }
    </tbody>
</table>

<div class="text-end">
    <button type="submit" class="btn btn-success">Rolleri Güncelle</button>
</div>
</form>
 *@